var SOCIALQ = {}
SOCIALQ.models = {}

SOCIALQ.models.pieChart = function() {
	
	function chart(selection) {
    	selection.each(function(data) {
			
			console.log(data)
			
		});
	}
	
	return chart;
}
